<?xml version="1.0" encoding="UTF-8"?>

<launch>
    <!-- REALSENSE CAMERA -->
    <include file="$(find realsense2_camera)/launch/rs_d435i_custom.launch"/>

    <!-- RECORD ROSBAG -->
    <node pkg="rosbag" type="record" name="data_capture"
          args="$(eval 'record --tcpnodelay --chunksize=4096 --buffsize 0 -O /media/usb/rosbags/' + raw_input('Enter rec file name: ') + '.bag
          /camera/imu
          /camera/color/image_raw
       ')"/>

</launch>
